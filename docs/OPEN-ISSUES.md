# 尚未完成的验收

最新证据见 [T6 实机记录](../results/20260923-t6/STATUS.md)。完整 amd64/ARM64 构建、T6 合成帧探测、真实 1080p60 双编码及独立解码、原版 Moonlight 配对和 HEVC 硬解显示已经验证。

## 当前阻碍

- USB 输入未启用。需核实实际 kvmd 鉴权、相对鼠标和独占输入范围；原绝对鼠标不能直接接收相对 delta。修改 HID descriptor 必须单列具体变更和回滚。
- 临时运行使用用户 ACL、独立运行库和手动窗口，尚无验收过的常驻部署及自动恢复方案。
- 源图像未采用可控色条/灰阶，虽 VUI 和独立解码通过，仍需源端与客户端色彩、缩放和画质对照。
- 客户端持续连接、重连的最终状态以实机记录为准，不能仅凭首帧认定长稳。

## 待扩展测试

拥塞/FEC/IDR恢复、断流清理、源模式变化、物理端到端延迟、24小时稳定性、1440p60/4K60。已有 60 秒独立解码证明本环境码流可解且无报告的 B 帧，不代表所有模式。

## 未实现范围

自动动态码率完整接入、RGA、NV16/NV24/多平面、绝对鼠标、HDMI音频/静音Opus、HDR/AV1/手柄。生产不增加桌面捕获或软件编码回退。

历史云端未构建状态保留在 results/cloud 与原交接文档供追溯，不是当前阻碍。
